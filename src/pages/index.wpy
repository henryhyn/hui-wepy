<template lang='pug'>
  view.hui-block
    block(wx:for='{{list}}' wx:key='item.path')
      navigator(url='{{item.path}}')
        cell(:bd='item.name')
</template>

<script>
  import wepy from 'wepy';
  import Cell from '@/components/Cell';

  export default class Index extends wepy.page {
    data = {
      list: [{
        name: 'Cell 单元格',
        path: '/examples/cell'
      }, {
        name: 'Panel 面板',
        path: '/examples/panel'
      }]
    };

    components = {
      cell: Cell
    };
  }
</script>
